#include <stdio.h>
/**
 * main - printsa given phrase
 * Return: 0 if exited right & non-zero otherwise
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
